[pytest]
# Only collect real tests under ./tests.
# Some helper scripts in ./scripts match pytest's default patterns (e.g. *_test.py)
# and may call sys.exit at import-time, which breaks the test run.
testpaths = tests
python_files = test_*.py

